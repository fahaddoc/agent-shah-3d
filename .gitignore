node_modules
dist
.DS_Store
*.log
.vite

# Mixamo FBX assets — too large for git (110MB+). Convert to GLB before committing.
public/assets/models/*.fbx

# Pistol starter pack (~58MB FBX/textures) — bundled separately
public/assets/pistol-starter/
.vercel
